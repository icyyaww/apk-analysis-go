# APK 动态分析平台 - 技术文档

> **项目说明：** 高性能 APK 自动化分析平台，基于 Go 语言重构，用于 Android 应用的静态分析、动态分析和流量捕获。

**文档最后更新：** 2025-11-25
**项目版本：** 1.0.0
**技术栈：** Go 1.23+ / Gin / GORM / MySQL / RabbitMQ / Redis / Docker / Python (Androguard)

---

## 📋 目录

1. [项目概述](#项目概述)
2. [技术架构](#技术架构)
3. [项目结构](#项目结构)
4. [核心业务流程](#核心业务流程)
5. [API 接口文档](#api-接口文档)
6. [数据库设计](#数据库设计)
7. [环境配置](#环境配置)
8. [启动部署](#启动部署)
9. [HTTPS 证书配置](#https-证书配置)
10. [故障排查](#故障排查)

---

## 项目概述

### 功能特性

- **静态分析**：
  - MobSF 静态安全扫描（可选）
  - **混合分析器**：Go 快速分析 + Python Androguard DEX 深度提取（推荐）
- **动态分析**：
  - 自动化 Activity 遍历
  - **AI 智能交互**（智谱 GLM-4V）：智能点击、输入、滑动操作
  - **实时可视化**：WebSocket 推送 AI 交互过程到前端
- **流量捕获**：Mitmproxy 拦截 HTTPS 流量 + Frida SSL Unpinning
- **域名分析**：域名归属地查询 + 备案信息查询 + 第三方 SDK 识别
- **智能过滤**：Activity 智能筛选，跳过系统组件
- **多设备支持**：
  - 设备池管理（模拟器 + 真机混合）
  - **APK 架构智能识别**：ARM-only APK 自动分配到真机
  - 设备健康检查 + 自动重连
- **可视化报告**：Activity 执行报告 + 静态分析报告 + 流量分析

### 性能指标

| 指标 | Python 版本 | Go 版本 | 提升 |
|------|------------|---------|------|
| 启动时间 | 3-5 秒 | < 500ms | **6-10x** |
| 内存占用 | ~2GB | < 300MB | **6x** |
| 并发任务 | 1-2 | 4-8 | **4x** |
| 单任务耗时 | 10-20 分钟 | 6-12 分钟 | **30-50%** |

---

## 技术架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         Frontend / API Client                    │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐ │
│   │  Dashboard  │  │ Task Detail │  │  AI Interaction (WS)    │ │
│   └─────────────┘  └─────────────┘  └─────────────────────────┘ │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Gin HTTP Server (Port 8080)                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────┐ │
│  │ Task API │ │ File API │ │Static API│ │ SDK API  │ │WS API │ │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └───────┘ │
└───────────────────────────────┬─────────────────────────────────┘
                                │
                    ┌───────────┴──────────────┐
                    ▼                          ▼
         ┌─────────────────┐        ┌──────────────────┐
         │  Task Service   │        │  Static Analyzer │
         └────────┬────────┘        │  (Hybrid Mode)   │
                  │                 └────────┬─────────┘
                  ▼                          │
         ┌─────────────────┐        ┌────────┴─────────┐
         │ RabbitMQ Queue  │        │ Go Fast Analyzer │
         └────────┬────────┘        │ + Python DEX     │
                  │                 │   (Androguard)   │
                  ▼                 └──────────────────┘
         ┌─────────────────┐
         │  Worker Pool    │
         │  (4 workers)    │
         └────────┬────────┘
                  │
                  ▼
         ┌─────────────────────────────────────────────┐
         │         Core Orchestrator (核心编排器)       │
         │  ┌────────────────────────────────────────┐ │
         │  │ 1. 智能设备选择 (ARM/x86 架构匹配)     │ │
         │  │ 2. 设备连接 (ADB) + 证书安装           │ │
         │  │ 3. APK 安装 + Frida SSL Unpinning      │ │
         │  │ 4. 代理设置 (per-device mitmproxy)     │ │
         │  │ 5. Monkey 预探索                       │ │
         │  │ 6. Activity 提取 & 智能过滤            │ │
         │  │ 7. AI 智能交互 (GLM-4V) + 实时推送     │ │
         │  │ 8. Activity 遍历 & 流量归因            │ │
         │  │ 9. 数据收集 & 清理                     │ │
         │  └────────────────────────────────────────┘ │
         └─────────┬───────────────────────────────────┘
                   │
     ┌─────────────┼─────────────────┬──────────────┐
     ▼             ▼                 ▼              ▼
┌─────────┐  ┌──────────┐     ┌──────────────┐  ┌────────────┐
│  MySQL  │  │  Redis   │     │  Mitmproxy   │  │  Devices   │
│  (RDS)  │  │  Cache   │     │ (1:8082)     │  │ ┌────────┐ │
└─────────┘  └──────────┘     │ (2:8084)     │  │ │Emulator│ │
                               └──────────────┘  │ │ (x86)  │ │
                                                 │ ├────────┤ │
                                                 │ │Real Dev│ │
                                                 │ │ (ARM)  │ │
                                                 │ └────────┘ │
                                                 └────────────┘
```

### 核心组件说明

#### 1. **Gin HTTP Server** - API 网关
- 路由管理：任务管理、文件服务、静态分析报告、SDK 规则
- 中间件：日志、CORS、Prometheus 监控
- WebSocket：AI 交互实时推送 (`/ws/ai-interaction/:task_id`)
- 健康检查：`/api/health`

#### 2. **Worker Pool** - 任务执行池
- 并发工作线程：4 个（可配置）
- 任务队列：从 RabbitMQ 消费任务
- 异步执行：每个任务独立执行，互不干扰

#### 3. **Device Manager** - 设备管理器
- **设备池**：支持多设备并发（模拟器 + 真机）
- **架构识别**：自动检测 APK 是否为 ARM-only
- **智能调度**：ARM APK 分配到真机，通用 APK 可用模拟器
- **健康检查**：定期检查设备连接状态，自动重连
- **设备休息**：每 N 个任务后自动休息，防止设备过热

#### 4. **Core Orchestrator** - 核心编排器
- **设备管理**：ADB 连接、应用安装/卸载
- **证书管理**：自动安装 mitmproxy 证书到 Android 系统
- **流量拦截**：配置代理 + Frida SSL Unpinning
- **AI 智能交互**：基于 GLM-4V 的智能 UI 分析和操作
- **Activity 遍历**：智能过滤 + 深度探索 + 流量归因
- **数据收集**：截图、UI 层级、Logcat、流量记录

#### 5. **Hybrid Static Analyzer** - 混合静态分析器
- **Go 快速分析**：提取 AndroidManifest.xml 基础信息
- **Python DEX 分析**：使用 Androguard 从 DEX 字节码提取 URL
- **智能决策**：根据 APK 大小和复杂度决定是否深度分析
- **URL 提取**：从代码字符串常量中提取完整 URL 列表

#### 6. **AI Interaction Engine** - AI 交互引擎
- **智谱 GLM-4V**：基于视觉的 UI 理解
- **智能点击**：识别可点击元素并生成操作策略
- **优先级排序**：根据元素重要性排序操作
- **实时广播**：通过 WebSocket 推送操作到前端可视化

#### 7. **Domain Analysis Service** - 域名分析服务
- **主域名提取**：从流量中提取应用主域名
- **备案查询**：调用站长工具 API 查询域名备案信息
- **IP 归属地**：查询域名 IP 的地理位置
- **第三方 SDK 识别**：匹配内置规则库，识别第三方服务

---

## 项目结构

```
apk-analysis-go/
├── cmd/
│   ├── server/               # 主服务入口
│   │   └── main.go          # 启动逻辑：初始化所有组件
│   ├── migrate/             # 数据库迁移工具
│   │   └── main.go
│   └── reanalyze/           # 重新分析工具
│       └── main.go
├── internal/                # 私有应用代码
│   ├── api/                 # HTTP API 层
│   │   ├── router.go        # 路由定义
│   │   └── handlers/        # API 处理器
│   │       ├── task_handler.go           # 任务管理 API
│   │       ├── file_handler.go           # 文件服务 API
│   │       ├── mobsf_handler.go          # MobSF 报告 API
│   │       ├── sdk_handler.go            # SDK 规则管理 API
│   │       ├── cert_handler.go           # 证书状态 API
│   │       ├── ai_interaction_handler.go # AI 交互 WebSocket
│   │       └── ai_broadcaster_adapter.go # AI 广播适配器
│   ├── domain/              # 领域模型
│   │   ├── task.go          # Task、Activity、MobSFReport 等数据模型
│   │   └── static_report.go # 静态分析报告模型
│   ├── repository/          # 数据访问层
│   │   ├── database.go      # 数据库初始化
│   │   ├── task_repo.go     # Task CRUD 操作
│   │   ├── sdk_repo.go      # SDK 规则 CRUD
│   │   └── static_report_repo.go  # 静态报告 CRUD
│   ├── service/             # 业务逻辑层
│   │   └── task_service.go  # 任务服务（创建、查询、删除）
│   ├── worker/              # 任务执行层
│   │   ├── pool.go          # Worker 池管理
│   │   └── orchestrator.go  # 核心编排器（任务执行流程）
│   ├── queue/               # 消息队列
│   │   ├── rabbitmq.go      # RabbitMQ 客户端
│   │   ├── producer.go      # 消息生产者
│   │   └── consumer.go      # 消息消费者
│   ├── device/              # 设备管理（新增）
│   │   └── device.go        # 设备池管理、架构检测、健康检查
│   ├── adb/                 # ADB 客户端
│   │   ├── client.go        # ADB 命令封装
│   │   └── connection_manager.go  # ADB 连接管理器
│   ├── mobsf/               # MobSF 集成
│   │   ├── client.go        # MobSF API 客户端
│   │   └── queue.go         # MobSF 队列管理器
│   ├── staticanalysis/      # 混合静态分析（新增）
│   │   ├── hybrid_analyzer.go   # 混合分析器主逻辑
│   │   ├── fast_analyzer.go     # Go 快速分析器
│   │   ├── process_pool.go      # Python 进程池
│   │   └── models.go            # 分析结果模型
│   ├── frida/               # Frida 动态注入
│   │   └── client.go        # Frida Server 管理 + SSL Unpinning
│   ├── cert/                # 证书安装
│   │   └── installer.go     # Mitmproxy 证书安装器
│   ├── flow/                # 流量分析
│   │   ├── parser.go        # 流量解析
│   │   └── attribution.go   # 流量归因（Activity 关联）
│   ├── filter/              # Activity 过滤
│   │   └── activity_filter.go  # 智能过滤规则
│   ├── domainanalysis/      # 域名分析
│   │   ├── analyzer.go      # 域名分析主逻辑
│   │   ├── service.go       # 域名分析服务
│   │   ├── beian.go         # 备案查询
│   │   ├── ip_location.go   # IP 归属地查询
│   │   ├── sdk_manager.go   # 第三方 SDK 识别
│   │   └── third_party_domains.go  # 第三方域名规则
│   ├── ip138/               # IP138 API 客户端（新增）
│   │   └── client.go        # IP 归属地查询
│   ├── ai/                  # AI 分析
│   │   ├── client.go        # 智谱 AI 客户端
│   │   ├── analyzer.go      # UI 智能分析
│   │   ├── interaction_engine.go  # AI 交互引擎
│   │   ├── smart_click.go   # 智能点击策略
│   │   └── ui_parser.go     # UI 层级解析
│   ├── config/              # 配置管理
│   │   ├── config.go        # 配置结构体
│   │   └── logger.go        # 日志初始化
│   ├── middleware/          # HTTP 中间件
│   │   ├── metrics.go       # 内存监控
│   │   ├── prometheus.go    # Prometheus 指标
│   │   └── pprof.go         # 性能分析
│   ├── retry/               # 重试机制（新增）
│   │   └── retry.go         # 通用重试工具
│   ├── utils/               # 工具函数
│   │   ├── dbpool.go        # 数据库连接池优化
│   │   └── stream.go        # 流式处理工具
│   └── watcher/             # 文件监控
│       └── file_watcher.go  # APK 目录监控
├── web/                     # 前端资源
│   ├── templates/           # HTML 模板
│   │   ├── dashboard.html   # 任务面板
│   │   ├── task_detail.html # 任务详情
│   │   ├── url_analysis.html# URL 分析页面
│   │   └── ai_interaction.html  # AI 交互监控页面
│   └── static/              # 静态资源
├── configs/                 # 配置文件
│   ├── config.yaml          # 主配置文件
│   ├── prometheus/          # Prometheus 配置
│   ├── alertmanager/        # AlertManager 配置
│   └── grafana/             # Grafana 仪表盘
├── deployments/             # 部署配置
│   ├── nginx/               # Nginx 配置
│   ├── prometheus/          # Prometheus 部署配置
│   ├── canary/              # 金丝雀发布配置
│   └── production/          # 生产部署脚本
├── scripts/                 # 脚本工具
│   ├── analyze_apk.py       # Androguard DEX 分析脚本
│   ├── androguard_deep_analyzer.py  # 深度分析脚本
│   ├── androguard_server.py # Androguard 服务器模式
│   ├── mitm_jsonl_writer.py # Mitmproxy 流量记录脚本
│   ├── wait_and_install_cert.sh    # 证书安装脚本
│   ├── install_user_cert.sh        # 用户证书安装
│   ├── init_certificates.sh        # 证书初始化
│   ├── download_frida_server.sh    # Frida 服务器下载
│   ├── ssl_unpinning.js            # Frida SSL Unpinning 脚本
│   ├── frida_ssl_unpinning_advanced.js  # 高级 SSL Unpinning
│   ├── migrate_sdk_rules.py        # SDK 规则迁移脚本
│   ├── emulator_health_monitor.sh  # 模拟器健康监控
│   └── setup_genymotion.sh         # Genymotion 设置
├── test/                    # 测试文件
│   ├── mode_switch_standalone_test.go
│   ├── performance_benchmark_test.go
│   └── simple_benchmark_test.go
├── tests/                   # 集成测试
│   ├── integration/         # 集成测试
│   └── stress/              # 压力测试
├── docs/                    # 文档目录
│   ├── swagger.yaml         # API 文档
│   ├── ARCHITECTURE.md      # 架构文档
│   ├── API.md               # API 文档
│   ├── DEPLOYMENT.md        # 部署文档
│   └── ...                  # 其他进度文档
├── venv/                    # Python 虚拟环境（Androguard）
├── docker-compose.prod.yml  # 生产环境 Docker Compose
├── docker-compose.monitoring.yml  # 监控服务 Compose
├── Dockerfile               # 主服务镜像
├── Makefile                 # 构建命令
├── .env.example             # 环境变量模板
└── go.mod                   # Go 依赖管理 (Go 1.23)
```

---

## 核心业务流程

### 任务执行完整流程

```
┌──────────────────────────────────────────────────────────────────┐
│ 1. 任务创建与入队                                                 │
│    - 监控 inbound_apks/ 目录                                     │
│    - 检测到新 APK 文件                                           │
│    - 创建 Task 记录（状态：queued）                              │
│    - 发布任务消息到 RabbitMQ                                     │
└────────────┬─────────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────────┐
│ 2. Worker Pool 消费任务                                          │
│    - Consumer 从 RabbitMQ 读取任务                               │
│    - 提交到 Worker Pool                                          │
│    - Worker 线程调用 Orchestrator.ExecuteTask()                 │
└────────────┬─────────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────────┐
│ 3. 智能设备分配                                                   │
│    ┌────────────────────────────────────────────────────────────┐│
│    │ 3.1 检测 APK 架构                                          ││
│    │     - 解析 lib/ 目录下的 .so 文件                          ││
│    │     - ARM-only: 只有 armeabi-v7a / arm64-v8a              ││
│    │     - 通用: 包含 x86 / x86_64                              ││
│    │ 3.2 选择合适设备                                           ││
│    │     - ARM-only APK → 分配真机 (OnePlus 5T)                ││
│    │     - 通用 APK → 可用模拟器或真机                          ││
│    │ 3.3 阻塞等待设备可用                                       ││
│    └────────────────────────────────────────────────────────────┘│
└────────────┬─────────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────────┐
│ 4. Orchestrator 核心编排（并行执行）                             │
│                                                                   │
│    ┌──────────────────────────────────────────────────────────┐ │
│    │ 并行任务 A：混合静态分析（Hybrid Analyzer）              │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step A.1: Go 快速分析                              │  │ │
│    │  │  - 解析 AndroidManifest.xml                        │  │ │
│    │  │  - 提取包名、版本、权限、组件列表                  │  │ │
│    │  │  - 计算 MD5/SHA256                                 │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step A.2: Python DEX 深度分析（Androguard）        │  │ │
│    │  │  - 分析所有 DEX 文件                               │  │ │
│    │  │  - 从字节码字符串常量提取 URL                      │  │ │
│    │  │  - 提取 Native 库列表                              │  │ │
│    │  │  - 分析证书信息                                    │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  - 保存静态分析报告到数据库                              │ │
│    └──────────────────────────────────────────────────────────┘ │
│                                                                   │
│    ┌──────────────────────────────────────────────────────────┐ │
│    │ 并行任务 B：动态分析（主线程）                            │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 1: 设备连接与证书安装                         │  │ │
│    │  │  - 连接分配的设备 (ADB)                            │  │ │
│    │  │  - 检查 mitmproxy 证书是否已安装                   │  │ │
│    │  │  - 未安装则自动安装到系统证书目录                  │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 2: APK 安装                                    │  │ │
│    │  │  - 预卸载同包名应用（避免冲突）                    │  │ │
│    │  │  - 安装 APK 到设备                                 │  │ │
│    │  │  - 提取包名并保存到数据库                          │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 3: Frida 注入（SSL Unpinning）                │  │ │
│    │  │  - 部署 frida-server 到设备                        │  │ │
│    │  │  - 启动 frida-server                               │  │ │
│    │  │  - 注入 SSL Unpinning 脚本到目标应用               │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 4: 网络代理配置                               │  │ │
│    │  │  - 设置设备专属代理                                │  │ │
│    │  │  - 真机：192.168.x.x:8082                         │  │ │
│    │  │  - 流量重定向到对应 Mitmproxy 实例                 │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 5: Monkey 预探索                              │  │ │
│    │  │  - 执行 100 次随机交互事件                         │  │ │
│    │  │  - 触发初始网络请求                                │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 6: Activity 提取与过滤                        │  │ │
│    │  │  - dumpsys package 提取所有组件                    │  │ │
│    │  │  - 过滤非 Activity 组件（Provider/Service/等）    │  │ │
│    │  │  - 智能过滤：跳过系统/第三方/无效 Activity        │  │ │
│    │  │  - 保存过滤报告                                    │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 7: Activity 遍历 + AI 智能交互                │  │ │
│    │  │  对每个 Activity：                                  │  │ │
│    │  │   1. 启动 Activity                                 │  │ │
│    │  │   2. 等待页面加载                                  │  │ │
│    │  │   3. 截图 + Dump UI 层级                           │  │ │
│    │  │   4. AI 分析 UI 生成交互策略                       │  │ │
│    │  │      - 解析可点击元素                              │  │ │
│    │  │      - 调用 GLM-4V 生成操作列表                    │  │ │
│    │  │      - WebSocket 实时推送到前端                    │  │ │
│    │  │   5. 执行 AI 推荐的操作（点击/滑动/输入）          │  │ │
│    │  │   6. 流量归因：关联当前 Activity 的网络请求        │  │ │
│    │  │   7. 返回主界面                                    │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 8: 后台监控（延迟请求）                       │  │ │
│    │  │  - 保持应用在前台运行 30 秒                        │  │ │
│    │  │  - 捕获周期性网络请求（如心跳、埋点）             │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    │  ┌────────────────────────────────────────────────────┐  │ │
│    │  │ Step 9: 数据收集与清理                             │  │ │
│    │  │  - 收集 logcat 日志                                │  │ │
│    │  │  - 清除代理配置                                    │  │ │
│    │  │  - 卸载应用（defer 保证一定执行）                  │  │ │
│    │  │  - 释放设备回设备池                                │  │ │
│    │  │  - 保存 Activity 详情到数据库                      │  │ │
│    │  └────────────────────────────────────────────────────┘  │ │
│    └──────────────────────────────────────────────────────────┘ │
└────────────┬─────────────────────────────────────────────────────┘
             │
             ▼
┌──────────────────────────────────────────────────────────────────┐
│ 5. 域名分析（任务完成后触发）                                    │
│    - 从 flows.jsonl 提取所有域名                                │
│    - 主域名识别（访问次数最多）                                  │
│    - 备案查询（调用站长工具 API）                                │
│    - IP 归属地查询                                               │
│    - 第三方 SDK 识别（匹配规则库）                               │
│    - 保存分析结果到数据库                                        │
└──────────────────────────────────────────────────────────────────┘
```

### 任务状态流转

```
queued (排队中)
   │
   ▼
installing (安装中: 连接设备 -> 安装证书 -> 安装 APK)
   │
   ▼
running (执行中: Monkey 测试 -> AI 交互 -> Activity 遍历 -> 后台监控)
   │
   ▼
collecting (收集中: 数据收集 -> 清理环境)
   │
   ├─────▶ completed (任务完成) ──▶ 触发域名分析
   │
   └─────▶ failed (任务失败)
```

---

## API 接口文档

### 基础信息

- **Base URL**: `http://localhost:8080/api`
- **响应格式**: JSON
- **时区**: UTC（响应中包含 `*_cst` 字段为 CST 时间）

### 任务管理 API

#### 1. 获取任务列表

```http
GET /api/tasks?limit=50
```

#### 2. 获取任务详情

```http
GET /api/tasks/:id
```

#### 3. 删除任务

```http
DELETE /api/tasks/:id
```

#### 4. 批量删除任务

```http
DELETE /api/tasks/batch
Content-Type: application/json

{
  "task_ids": ["id1", "id2"],
  "status": "completed",
  "before_days": 7
}
```

#### 5. 停止任务

```http
POST /api/tasks/:id/stop
```

#### 6. 获取系统统计

```http
GET /api/stats
```

### 文件服务 API

#### 7. APK 上传

```http
POST /api/upload
Content-Type: multipart/form-data
```

#### 8. 获取截图

```http
GET /api/tasks/:id/screenshot/:filename
```

#### 9. 列出所有截图

```http
GET /api/tasks/:id/screenshots
```

#### 10. 获取 UI 层级文件

```http
GET /api/tasks/:id/ui_hierarchy/:filename
```

#### 11. 下载流量记录

```http
GET /api/tasks/:id/flows
```

### 静态分析报告 API

#### 12. 获取 MobSF 报告（JSON）

```http
GET /api/tasks/:id/mobsf
```

#### 13. 获取 MobSF 报告（HTML）

```http
GET /api/tasks/:id/mobsf/report
```

#### 14. 获取混合静态分析报告（JSON）

```http
GET /api/tasks/:id/static
```

#### 15. 获取混合静态分析报告（HTML）

```http
GET /api/tasks/:id/static/report
```

#### 16. 获取 MobSF 队列状态

```http
GET /api/mobsf/queue
```

### Activity 报告 API

#### 17. 获取 Activity URL 列表

```http
GET /api/tasks/:id/urls
```

#### 18. 获取单个 Activity 的 URL

```http
GET /api/tasks/:id/activities/:name/urls
```

#### 19. 获取 Activity 执行报告（HTML）

```http
GET /api/tasks/:id/activities/report
```

### 设备管理 API

#### 20. 获取设备状态

```http
GET /api/devices
```

**Response**:
```json
{
  "devices": [
    {
      "id": "oneplus-5t",
      "adb_target": "192.168.2.38:5555",
      "arch": "arm",
      "in_use": false,
      "current_task_id": "",
      "tasks_completed": 15
    }
  ],
  "total": 1,
  "available": 1
}
```

### 证书状态 API

#### 21. 获取证书状态

```http
GET /api/cert/status
```

### SDK 规则管理 API

#### 22. 获取 SDK 规则列表

```http
GET /api/sdk_rules
```

#### 23. 创建 SDK 规则

```http
POST /api/sdk_rules
```

#### 24. 获取待审核 SDK 规则

```http
GET /api/sdk_rules/pending
```

#### 25. 获取 SDK 统计

```http
GET /api/sdk_rules/statistics
```

#### 26. 获取 SDK 分类列表

```http
GET /api/sdk_rules/categories
```

#### 27. 批准/拒绝 SDK 规则

```http
POST /api/sdk_rules/:id/approve
POST /api/sdk_rules/:id/reject
```

#### 28. 更新/删除 SDK 规则

```http
PUT /api/sdk_rules/:id
DELETE /api/sdk_rules/:id
```

### AI 交互 WebSocket

#### 29. AI 交互监控页面

```http
GET /ai-interaction
```

#### 30. AI 交互 WebSocket

```
WS /ws/ai-interaction/:task_id
```

**消息格式**:
```json
{
  "type": "action",
  "activity": "MainActivity",
  "data": {
    "type": "click",
    "x": 540,
    "y": 960,
    "reason": "点击登录按钮",
    "priority": 10
  }
}
```

---

## 数据库设计

### 表结构

#### 1. apk_tasks - 主任务表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 主键，UUID |
| apk_name | VARCHAR(255) | APK 文件名 |
| package_name | VARCHAR(255) | 应用包名 |
| status | VARCHAR(20) | 任务状态 |
| should_stop | BOOLEAN | 停止标记 |
| error_message | TEXT | 错误信息 |
| created_at | TIMESTAMP | 创建时间 |
| started_at | TIMESTAMP | 开始时间 |
| completed_at | TIMESTAMP | 完成时间 |
| current_step | VARCHAR(255) | 当前步骤 |
| progress_percent | TINYINT | 进度百分比 |
| device_connected | BOOLEAN | 设备连接状态 |
| static_analysis_completed | BOOLEAN | 静态分析完成标记 |
| dynamic_analysis_completed | BOOLEAN | 动态分析完成标记 |

#### 2. task_activities - Activity 信息表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| task_id | VARCHAR(36) | 关联任务 ID |
| launcher_activity | VARCHAR(500) | 启动 Activity |
| activities_json | TEXT | Activity 列表 |
| activity_details_json | MEDIUMTEXT | Activity 详情（JSON 数组） |
| created_at | TIMESTAMP | 创建时间 |

#### 3. task_mobsf_reports - MobSF 报告表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| task_id | VARCHAR(36) | 关联任务 ID |
| status | VARCHAR(30) | 分析状态 |
| hash | VARCHAR(64) | APK Hash |
| score | TINYINT | 安全评分 (0-100) |
| app_name | VARCHAR(255) | 应用名称 |
| report_json | MEDIUMTEXT | 完整报告 JSON |
| scan_progress | VARCHAR(500) | 扫描进度描述 |
| progress_detail | TEXT | 详细进度日志 |
| scanned_at | TIMESTAMP | 扫描完成时间 |

#### 4. task_static_reports - 混合静态分析报告表（新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| task_id | VARCHAR(36) | 关联任务 ID |
| analyzer | VARCHAR(20) | 分析器类型：go_only/hybrid |
| analysis_mode | VARCHAR(20) | 分析模式：fast/deep/fast_fallback |
| status | VARCHAR(30) | 分析状态 |
| package_name | VARCHAR(255) | 包名 |
| version_name | VARCHAR(50) | 版本名 |
| version_code | VARCHAR(20) | 版本号 |
| app_name | VARCHAR(255) | 应用名称 |
| file_size | BIGINT | 文件大小 |
| md5 | VARCHAR(32) | MD5 哈希 |
| sha256 | VARCHAR(64) | SHA256 哈希 |
| activity_count | INT | Activity 数量 |
| service_count | INT | Service 数量 |
| receiver_count | INT | Receiver 数量 |
| provider_count | INT | Provider 数量 |
| permission_count | INT | 权限数量 |
| url_count | INT | URL 数量 |
| domain_count | INT | 域名数量 |
| basic_info_json | TEXT | 基础信息 JSON |
| deep_analysis_json | MEDIUMTEXT | 深度分析 JSON (URLs, Native libs) |
| analysis_duration_ms | INT | 总分析耗时 |
| fast_analysis_duration_ms | INT | 快速分析耗时 |
| deep_analysis_duration_ms | INT | 深度分析耗时 |
| needs_deep_analysis_reason | VARCHAR(255) | 深度分析原因 |
| analyzed_at | TIMESTAMP | 分析完成时间 |

#### 5. task_domain_analysis - 域名分析表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| task_id | VARCHAR(36) | 关联任务 ID |
| primary_domain | VARCHAR(255) | 主域名 |
| primary_domain_json | TEXT | 主域名详情 JSON |
| domain_beian_status | VARCHAR(50) | 备案状态 |
| domain_beian_json | TEXT | 备案信息 JSON |
| app_domains_json | TEXT | 所有域名列表 JSON |
| url_analysis_static | MEDIUMTEXT | 静态 URL 分析 |
| url_analysis_dynamic | MEDIUMTEXT | 动态 URL 分析 |
| analyzed_at | TIMESTAMP | 分析完成时间 |

#### 6. task_app_domains - 域名归属地表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| task_id | VARCHAR(36) | 关联任务 ID |
| domain | VARCHAR(255) | 域名 |
| ip | VARCHAR(45) | IP 地址 |
| province | VARCHAR(50) | 省份 |
| city | VARCHAR(50) | 城市 |
| isp | VARCHAR(50) | 运营商 |
| source | VARCHAR(50) | 数据来源 |

#### 7. task_ai_logs - AI 交互日志表（新增）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| task_id | VARCHAR(36) | 关联任务 ID |
| interaction_json | MEDIUMTEXT | AI 交互记录 JSON |
| created_at | TIMESTAMP | 创建时间 |

#### 8. third_party_sdk_rules - 第三方 SDK 规则表

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| domain | VARCHAR(255) | 域名（唯一） |
| category | VARCHAR(100) | 类别：analytics/ad/... |
| sub_category | VARCHAR(50) | 子类别 |
| provider | VARCHAR(255) | 提供商名称 |
| description | TEXT | 描述 |
| source | VARCHAR(20) | 来源：builtin/discovered/manual |
| confidence | DECIMAL(3,2) | 置信度 0.00-1.00 |
| status | VARCHAR(20) | 状态：active/pending/disabled |
| discover_count | INT | 发现次数 |
| first_seen_task_id | VARCHAR(36) | 首次发现的任务 ID |
| priority | TINYINT | 优先级 0-100 |
| created_by | VARCHAR(100) | 创建者 |
| updated_by | VARCHAR(100) | 更新者 |

---

## 环境配置

### 主配置文件 (configs/config.yaml)

```yaml
server:
  port: 8080
  mode: release  # debug, release

database:
  type: mysql
  host: rm-cn-xxx.rds.aliyuncs.com
  port: 3306
  user: root
  password: your_password
  db_name: apk_analysis_go

redis:
  host: redis
  port: 6379

rabbitmq:
  host: rabbitmq
  port: 5672
  user: user
  password: pass
  vhost: /
  queue: apk_tasks

# 静态分析配置
static_analysis:
  # 可选值: mobsf / hybrid / both
  enabled_analyzers: hybrid  # 推荐使用混合分析

  mobsf:
    enabled: false  # 禁用 MobSF（使用混合分析器）
    url: http://mobsf:8000
    api_key: "your_api_key"
    timeout: 7200
    http_timeout: 7200

  hybrid:
    enabled: true
    python_path: /usr/bin/python3
    script_path: /app/scripts/analyze_apk.py
    use_process_pool: false
    process_pool_size: 3
    force_deep_analysis: true  # 始终进行 DEX 深度分析

# AI 智能交互配置
ai:
  enabled: true
  api_key: your_glm_api_key
  model: glm-4v-flash
  max_actions: 20

# ADB 配置
adb:
  target: android-emulator:5555
  timeout: 30

# Mitmproxy 配置
mitmproxy:
  host: mitmproxy
  port: 8082

# Worker 配置
worker:
  concurrency: 4
  queue_size: 100

# 日志配置
log:
  level: info
  format: text

# 备案查询配置
beian:
  enabled: true
  api_key: "your_chinaz_api_key"
  api_url: "http://openapiu67.chinaz.net/v1/1001/icpappunit"
  timeout: 70

# 目录配置
apk_dir: ./inbound_apks
result_dir: ./results
data_dir: ./data
```

### 环境变量 (.env)

```bash
# 数据库
MYSQL_HOST=rm-cn-xxx.rds.aliyuncs.com
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASS=your_password
MYSQL_DB=apk_analysis_go

# RabbitMQ
RABBITMQ_USER=user
RABBITMQ_PASS=pass

# MobSF (可选)
MOBSF_API_KEY=your_mobsf_api_key

# AI 服务
GLM_API_KEY=your_glm_api_key
AI_INTERACTION_ENABLED=true

# Grafana
GRAFANA_ADMIN_PASSWORD=your_grafana_password
```

### Docker Compose 服务配置 (docker-compose.prod.yml)

| 服务 | 端口 | 说明 |
|------|------|------|
| apk-analysis-server | 8080 | 主应用服务（Go） |
| rabbitmq | 5672, 15672 | 消息队列 |
| redis | 6379 | 缓存服务 |
| mobsf | 8000 | MobSF 静态分析（可选） |
| mitmproxy-1 | 8082, 8083 | 流量拦截代理 #1 |
| mitmproxy-2 | 8084, 8085 | 流量拦截代理 #2 |
| android-emulator-1 | 5555, 6080 | Android 模拟器 #1 |
| android-emulator-2 | 5557, 6081 | Android 模拟器 #2 |
| prometheus | 9091 | 监控数据采集 |
| grafana | 3000 | 可视化监控面板 |
| nginx | 80, 443 | 反向代理 |

---

## 启动部署

### 前置要求

1. **操作系统**：Linux (Ubuntu 20.04+ 推荐)
2. **Docker & Docker Compose**：20.10+
3. **Python 3.12+**（用于 Androguard）
4. **Go 1.23+**（开发编译）
5. **系统要求**：
   - CPU: 8 核以上
   - 内存: 16GB 以上
   - 硬盘: 100GB 以上
6. **KVM 支持**（用于 Android 模拟器）

### 快速启动

```bash
# 1. 克隆项目
git clone <repository-url>
cd apk-analysis-go

# 2. 配置环境变量
cp .env.example .env
vim .env

# 3. 安装 Python 依赖（Androguard）
python3 -m venv venv
source venv/bin/activate
pip install androguard>=4.1.3

# 4. 启动所有服务
docker compose -f docker-compose.prod.yml up -d

# 5. 检查服务状态
docker compose -f docker-compose.prod.yml ps

# 6. 查看日志
docker compose -f docker-compose.prod.yml logs -f apk-analysis-server

# 7. 访问服务
# - API: http://localhost:8080
# - Dashboard: http://localhost:8080/
# - AI 交互监控: http://localhost:8080/ai-interaction
# - Grafana: http://localhost:3000
# - RabbitMQ: http://localhost:15672
```

### 真机设备配置

如果使用真机进行动态分析：

```bash
# 1. 连接真机（WiFi ADB）
adb tcpip 5555
adb connect 192.168.x.x:5555

# 2. 安装 mitmproxy 证书
# 方法1：使用 Magisk 模块
adb push /tmp/MitmproxyCert.zip /sdcard/Download/
adb shell su -c "magisk --install-module /sdcard/Download/MitmproxyCert.zip"
adb reboot

# 方法2：用户证书 + Frida（无需 root）
bash scripts/install_user_cert.sh 192.168.x.x:5555 apk-analysis-mitmproxy-1

# 3. 在 main.go 中配置设备
device1 := &device.Device{
    ID:                 "my-phone",
    ADBTarget:          "192.168.x.x:5555",
    ProxyHost:          "192.168.y.y",  // 宿主机 IP
    ProxyPort:          8082,
    MitmproxyContainer: "apk-analysis-mitmproxy-1",
    MitmproxyAPIPort:   8083,
    Arch:               device.ArchARM,
}
```

---

## HTTPS 证书配置

### 证书配置方案

**当前推荐：系统证书（Magisk 真机）+ Frida SSL Unpinning**

| 设备类型 | 证书方案 | 说明 |
|---------|---------|------|
| 真机 (Magisk) | 系统证书 | 通过 Magisk 模块安装到 /system |
| 真机 (无 root) | 用户证书 + Frida | 配合 SSL Unpinning 使用 |
| 模拟器 | 系统证书 | 重新挂载 /system 为可写 |

### 证书安装脚本

```bash
# 真机（Magisk）
bash scripts/install_user_cert.sh <device:port> <mitmproxy-container>

# 模拟器
bash scripts/wait_and_install_cert.sh <device:port>
```

---

## 故障排查

### 1. APK 架构不匹配

**症状**：ARM-only APK 在模拟器上安装失败

**解决**：
- 确保有 ARM 架构的真机设备
- 检查 `device.DetectAPKArch()` 函数
- 查看日志中的架构检测结果

### 2. AI 交互无响应

**症状**：AI 智能交互不工作

**排查**：
```bash
# 1. 检查 GLM API Key
echo $GLM_API_KEY

# 2. 检查 AI 配置
grep -A5 "ai:" configs/config.yaml

# 3. 查看 AI 相关日志
docker logs apk-analysis-server | grep -i "ai\|glm"
```

### 3. Androguard 分析失败

**症状**：静态分析报告缺少 URL

**排查**：
```bash
# 1. 检查 Python 环境
source venv/bin/activate
python -c "import androguard; print(androguard.__version__)"

# 2. 手动测试分析脚本
python scripts/analyze_apk.py /path/to/test.apk
```

### 4. 设备健康检查失败

**症状**：设备频繁离线

**排查**：
```bash
# 1. 检查 ADB 连接
adb devices

# 2. 查看设备状态 API
curl http://localhost:8080/api/devices

# 3. 检查 ADB 连接管理器日志
docker logs apk-analysis-server | grep "ADB\|device\|health"
```

---

## 附录

### 常用命令

```bash
# 构建（本地开发）
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags="-w -s" -o ./apk-analysis-server ./cmd/server

# ⚠️ 重要：部署到容器
# 容器启动命令是 ./apk-analysis-server，必须复制到 /app/apk-analysis-server
# 不要复制到 /app/server，否则新代码不会被执行！
docker cp ./apk-analysis-server apk-analysis-server:/app/apk-analysis-server && docker restart apk-analysis-server

# 测试
go test ./...

# 格式化
go fmt ./...

# 数据库迁移
go run cmd/migrate/main.go

# 重新分析任务
go run cmd/reanalyze/main.go <task_id>
```

### 监控端点

```bash
# Prometheus 指标
curl http://localhost:8080/metrics/prometheus

# 内存统计
curl http://localhost:8080/metrics

# 强制 GC
curl -X POST http://localhost:8080/debug/gc

# 设备状态
curl http://localhost:8080/api/devices

# 证书状态
curl http://localhost:8080/api/cert/status
```

### 相关文档

- [Go 语言官方文档](https://go.dev/doc/)
- [Gin Web 框架](https://gin-gonic.com/docs/)
- [GORM ORM](https://gorm.io/docs/)
- [Androguard 文档](https://androguard.readthedocs.io/)
- [智谱 AI API](https://open.bigmodel.cn/dev/api)
- [Frida 文档](https://frida.re/docs/home/)
- [Mitmproxy 文档](https://docs.mitmproxy.org/stable/)

---

## 文档索引

### 进度文档

| 文档名称 | 功能描述 | 状态 |
|---------|---------|------|
| docs/IP归属地功能实现进度.md | IP138 API 集成 | 已完成 |
| docs/混合静态分析方案（Go+Python）.md | Hybrid 分析器设计 | 已完成 |
| docs/多设备并发方案设计.md | 设备池设计 | 已完成 |
| docs/双模拟器热备份方案实施文档.md | 双模拟器配置 | 已完成 |
| docs/证书自动安装方案.md | 证书安装方案 | 已完成 |
| docs/ADB并发冲突解决方案.md | ADB 连接管理 | 已完成 |
| docs/AI_SMART_INTERACTION_INTEGRATION.md | AI 智能交互集成 | 已完成 |

---

**文档维护者：** APK Analysis Team
**最后更新：** 2025-11-25
